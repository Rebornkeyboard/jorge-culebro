class Product {
    constructor(id, name, description, height, length, width) {
        (this.id = id),
        (this.name = name),
        (this.description = description),
        (this.height = height),
        (this.length = length),
        (this.width = width);
    }
  }
  
  export default Product;