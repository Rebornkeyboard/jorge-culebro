class User {
    constructor(id, dataID, token, name, phone, profileImage) {
        (this.id = id),
        (this.dataID = dataID),
        (this.token = token),
        (this.name = name),
        (this.phone = phone),
        (this.profileImage = profileImage);
    }
  }
  
  export default User;